// The modal edit dialog was replaced by the page-style Configuration tab
// (ScanConfigurationTab) in the data-first redesign. This re-export is kept so
// any lingering import path still resolves to the canonical edit surface.
export { ScanConfigurationTab as ScanEditView } from './ScanConfigForm'
