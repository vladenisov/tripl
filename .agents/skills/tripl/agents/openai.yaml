interface:
  display_name: "Tripl"
  short_description: "Repo-specific patterns and workflows for tripl"
  default_prompt: "Use the tripl repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true